_schema-version: "3.2"
ID: ui5dev
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: ui5dev-destination-content
  type: com.sap.application.content
  requires:
  - name: ui5dev-destination-service
    parameters:
      content-target: true
  - name: ui5dev-repo-host
    parameters:
      service-key:
        name: ui5dev-repo-host-key
  - name: ui5dev-uaa
    parameters:
      service-key:
        name: ui5dev-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: ui5dev_html_repo_host
          ServiceInstanceName: ui5dev-html5-srv
          ServiceKeyName: ui5dev-repo-host-key
          sap.cloud.service: ui5dev
        - Authentication: OAuth2UserTokenExchange
          Name: ui5dev_uaa
          ServiceInstanceName: ui5dev-xsuaa-srv
          ServiceKeyName: ui5dev-uaa-key
          sap.cloud.service: ui5dev
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: ui5dev-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ui5dev-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ui5dev.zip
      name: ui5dev
      target-path: resources/
- name: ui5dev
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ui5dev-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ui5dev-destination-service
    service-plan: lite
- name: ui5dev-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: ui5dev-xsuaa-srv
    service-plan: application
- name: ui5dev-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ui5dev-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
